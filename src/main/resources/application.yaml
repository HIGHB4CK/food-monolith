spring:
  application:
    name: food-monolith
  datasource:
    url: jdbc:postgresql://localhost:5432/food_delivery
    username: postgres
    password: Guseynov2005
  flyway:
    enabled: true
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
